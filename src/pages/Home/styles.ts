import styled from "styled-components"

export const Home = styled.section`

  picture{
    display: flex;
    flex-direction: row;

    div{
      display:flex;
      flex-direction: column;
      padding:5rem;
    }
  }
`